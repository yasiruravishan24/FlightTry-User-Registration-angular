import { UniqueHostDirective } from './unique-host.directive';

describe('UniqueHostDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueHostDirective();
    expect(directive).toBeTruthy();
  });
});
